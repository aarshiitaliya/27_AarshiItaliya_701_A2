<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Redis Session Store</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <div class="login-form">
            <div class="form-header">
                <h1>🔐 Login</h1>
                <p>Redis Session Store Demo</p>
            </div>

            <% if (error) { %>
                <div class="alert alert-error">
                    <strong>Error:</strong> <%= error %>
                </div>
            <% } %>

            <% if (success) { %>
                <div class="alert alert-success">
                    <strong>Success:</strong> <%= success %>
                </div>
            <% } %>

            <form method="POST" action="/login">
                <div class="form-group">
                    <label for="username">Username:</label>
                    <input type="text" id="username" name="username" required>
                </div>

                <div class="form-group">
                    <label for="password">Password:</label>
                    <input type="password" id="password" name="password" required>
                </div>

                <button type="submit" class="btn btn-primary">Login</button>
            </form>

            <div class="demo-accounts">
                <h3>Demo Accounts:</h3>
                <div class="account-info">
                    <strong>Admin:</strong> username=<code>admin</code>, password=<code>password</code>
                </div>
                <div class="account-info">
                    <strong>User:</strong> username=<code>user</code>, password=<code>password</code>
                </div>
            </div>

            <div class="tech-info">
                <h4>🚀 Technology Stack:</h4>
                <ul>
                    <li>Express.js with express-session</li>
                    <li>Redis session store (connect-redis)</li>
                    <li>bcryptjs for password hashing</li>
                    <li>express-validator for input validation</li>
                    <li>EJS templating engine</li>
                </ul>
            </div>
        </div>
    </div>
</body>
</html>
